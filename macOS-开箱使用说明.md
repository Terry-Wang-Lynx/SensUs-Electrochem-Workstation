# SensUs 电化学工作站 macOS 开箱使用说明

## 适用环境

- macOS 14 或更高版本
- Apple Silicon（M1 及后续芯片）
- J-Link 连接的 V4 板卡，或 USB DATA/SMP 连接的 V5.1 板卡

DMG 已包含上位机、Python 运行环境、OpenOCD 和可运行时配置的稳定固件。
正常使用不需要联网，也不需要安装 Homebrew、Python、SEGGER 或固件工具链。

## 第一次使用

1. 打开 `SensUs-Workstation-macOS-arm64-<版本>.dmg`，把 `SensUs Workstation`
   拖入“应用程序”。
2. 当前发布包尚未使用 Apple Developer ID 公证。首次启动时，在 Finder
   的“应用程序”中右键 `SensUs Workstation`，选择“打开”，再确认一次。
3. 选择一个可写文件夹作为数据工作区。软件会记住该目录，并为每个
   测量批次创建独立子目录；未选工作区时不能开始正式测量。
4. 完成后请点页面右上角“退出”。软件会先停止测量、释放 J-Link/USB，
   再关闭原生窗口和后端。

## 连接与测量

1. 给目标板正常供电后再接入 J-Link 或 USB 数据线。左下角会显示实际连接方式
   和目标板状态。
2. 只有一个可用设备时会自动选择；同时接入多个 USB/J-Link 时，在右上角
   “选择设备”中手动指定。
3. 在“方法条件”中选择预设或修改参数，再点“应用条件并烧录硬件”。
   软件会先识别现有固件；支持运行时配置时直接下发参数，仅在需要时更新固件。
4. 输入样品和批次信息后开始测量。CSV、摘要、图表及当时的条件和滤波设置
   都会保存在当前批次目录中。

## 常见问题

- 找不到 J-Link：确认 J-Link 的 USB 线可传数据、目标板供电和 VTref 正常，
  并关闭 Ozone、IDE、J-Link Commander 等可能占用探针的软件。
- J-Link 在线但目标板无响应：检查板卡供电、SWDIO/SWCLK/GND/VTref 和排线方向。
- USB 板卡未就绪：确认使用数据线，等待 DATA 与 SMP 两个 CDC 接口完成枚举；
  更新固件后软件会自动等待它们重连。
- 页面提示旧版本：从右上角“退出”后重新打开 App，不要同时启动多个副本。
- 诊断日志位于 `~/Library/Logs/SensUs Workstation/`。报错时请保留页面给出的诊断编号。

发布包不包含发送者的历史测量、样品名称、标定模型或本机路径。
