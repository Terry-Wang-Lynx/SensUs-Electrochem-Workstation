# SensUs 电化学工作站 macOS 开箱使用说明

## 适用环境

- macOS 13 或更高版本
- Apple Silicon（M1/M2/M3/M4/M5）或 Intel Mac
- 首次安装需要联网，并建议预留约 2 GB 空间
- 测量时需要 J-Link、pA-Converter 板及可靠的板端供电

请先完整解压 ZIP，再把整个文件夹放到本机的“下载”或“文稿”目录。不要在 ZIP
预览窗口里运行，也不要只把 `.app` 单独拖走。建议避免直接放在微信临时目录、
OneDrive 或 iCloud 同步目录中。

## 第一次使用

1. 右键点击 `01-首次安装.command`，选择“打开”。
2. 若 macOS 提示无法验证开发者，再次选择“打开”。安装过程中可能要求输入 Mac
   登录密码；密码输入时终端不会显示字符，这是正常现象。
3. 脚本会自动安装 Homebrew、Python 3.12、OpenOCD 和软件依赖，并完成自检。
   首次安装通常需要 5 至 20 分钟，取决于网络和电脑是否已有 Homebrew。
4. 安装完成后工作站会自动打开。以后只需双击 `02-启动工作站.command`。

随时可以双击 `04-环境自检.command` 检查软件、推荐固件、OpenOCD 和 J-Link。
J-Link 未插入时自检会显示“未插入”，但不影响软件安装。

## 最简可用测量流程

1. 给 pA-Converter 板正常供电，将 J-Link 连接至板子的 SWD 接口，并把 J-Link
   插入 Mac。只连接一个探头时无需填写序列号。
2. 启动工作站，使用界面默认的推荐 I-T 条件：
   `+0.2 V`、`180 s`、`2 µA FSR`、`10% FSR（200 nA）offset`、`10 Hz`。
3. 点击“应用条件并烧录硬件”。随包固件会直接写入，无需安装完整固件工具链。
   烧录会复位 MCU，因此不要在一轮测量过程中执行此操作。
4. 选择保存目录，填写样品名称；做标定样品时再填写已知浓度并选择“标定”。
5. 点击“开始测量”，等待 180 秒结束。原始 CSV、处理后 CSV、摘要 JSON 和曲线图
   会保存在所选目录；默认目录是 `measurements/experiment_data`。

推荐条件的主机输出目标是 10 Hz。硬件原生转换周期约 124 ms，因此实际原生速率
约为 8.06 Hz；软件不会凭空补出新的硬件样本。

## 何时运行 03

仅当需要把电位、时长、量程、offset 或 CV 条件改成随包推荐值以外的配置时，
才双击 `03-安装固件工具链.command`。它会安装 nRF Connect SDK v3.4.0 和
Zephyr SDK 1.0.1，建议额外预留约 8 GB，下载和编译可能需要较长时间。

只使用上述推荐 I-T 条件时，不需要运行 03。

## 常见问题

- `01` 双击后只显示文本：右键文件，选择“打开方式”→“终端”。
- 提示没有权限：在终端执行 `chmod +x `，把对应 `.command` 文件拖入终端后回车。
- 找不到 J-Link：确认板子供电、USB 数据线可传数据，并断开其他占用探头的软件。
- 页面看似是旧版本：退出工作站后重新双击 `02`；浏览器模式下可强制刷新页面。
- 安装失败：保留终端窗口，将最后 30 行错误信息发给维护者。

交付包不含发送者的历史测量、样品名称、标定模型或本机路径。每台 Mac 会在自己的
文件夹内新建独立运行环境和测量数据。
